{
  "blocks": [
    {
      "type": "section",
      "text": {
        "type": "mrkdwn",
        "text": ":no_entry: <${CIRCLE_BUILD_URL}|Pipeline $CIRCLE_BUILD_NUM> *failed* tests for `${CIRCLE_BRANCH}` @ `${CIRCLE_SHA1:0:9}` by ${CIRCLE_USERNAME}"
      }
    }
  ]
}
